{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export PYTHONPATH=$PYTHONPATH:. && python -m uvicorn app.main:app --host 0.0.0.0 --port 3000"
  }
}
